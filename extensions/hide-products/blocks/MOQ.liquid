  {% schema %}
  {
    "name": "MOQ",
    "available_if": "{{ app.metafields.shipturtle.minimum_order_qty_availability_status }}",
    "target": "body",
    "stylesheet": "moq.css",
    "javascript" : "moq.js",
    "settings": [
     
  
    ]
  }
  {% endschema %}
  <script>
    var cartItems = {
      items: [
        {% for item in cart.items %}
          {
            productTags: {{ item.product.tags | json }},
          }{% unless forloop.last %},{% endunless %}
        {% endfor %}
      ]
    };
    var productObject ={
      productTag : {{ product.tags | json }},
    }
  </script>
  